
## Getting started

```shell
npm install
npm run start /build project, browser-sync server on localhost:3000, if gulp not found install it globally wih 'npm install -g gulp'/
npm run start:server (node server on localhost:9000)
```